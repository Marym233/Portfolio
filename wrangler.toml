# Cloudflare Workers config.
# The site is just files, so there's no server code — Workers serves
# everything in dist/ straight from the edge.

name = "maryam-portfolio"
compatibility_date = "2026-01-01"

[assets]
directory = "./dist"
not_found_handling = "404-page"
