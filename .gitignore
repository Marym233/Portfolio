node_modules/
dist/
.wrangler/
.DS_Store

# Secrets — never commit these. .dev.vars is where wrangler keeps local
# secrets, and it is the easiest file in this project to leak by accident.
.env
.env.*
.dev.vars
.dev.vars.*
*.pem
*.key

# Local tooling, not part of the site
.claude/
.vscode/
.idea/

# Noise
*.log
npm-debug.log*
pnpm-debug.log*
.playwright-mcp/
Thumbs.db
